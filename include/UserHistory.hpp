#ifndef USER_HISTORY_HPP
#define USER_HISTORY_HPP

/**
 * @file UserHistory.hpp
 * @brief Definição da classe UserHistory para armazenar histórico de usuário.
 */

/**
 * @brief Classe para armazenar o histórico de ações do usuário.
 */
class UserHistory
{
public:
    /**
     * @brief Construtor da classe UserHistory.
     *
     * Este construtor imprime o histórico de ações do usuário no sistema.
     */
    UserHistory();

    /**
     * @brief Destrutor da classe UserHistory.
     */
    ~UserHistory();
};

#endif // USER_HISTORY_HPP
